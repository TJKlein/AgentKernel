[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
    --strict-markers
    --strict-config
markers =
    live: marks tests as requiring real LLM (deselect with '-m "not live"')

